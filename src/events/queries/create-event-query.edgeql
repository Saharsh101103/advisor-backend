INSERT Event{
    title := <str>$title,
    date := <datetime>$date,
    time := <str>$time,
    type := <EventType>$type,
    description := <str>$description,
    location := <optional str>$location,
  }
